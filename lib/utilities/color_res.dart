hereimport 'package:flutter/material.dart';

class ColorRes {
  static const Color blackPure      = Color(0xFF0D0D0D);
  static const Color whitePure      = Color(0xFFFFFFFF);

  // ─── Backgrounds ────────────────────────────────────
  static const Color bgDeep         = Color(0xFFF8F9FF);
  static const Color themeColor     = Color(0xFFFFFFFF);
  static const Color themeGradient2 = Color(0xFFF3F4FF);
  static const Color themeGradient1 = Color(0xFFEEEFFF);
  static const Color themeAccentSolid = Color(0xFFE8EAFF);

  // ─── Borders ────────────────────────────────────────
  static const Color borderSubtle   = Color(0xFFDDDFFF);
  static const Color borderMedium   = Color(0xFFCCCFFF);

  // ─── Text ───────────────────────────────────────────
  static const Color textPrimary    = Color(0xFF0D0D0D);
  static const Color textSecondary  = Color(0xFF444466);
  static const Color textMuted      = Color(0xFF9999BB);
  static const Color textDarkGrey   = Color(0xFF1A1A2E);
  static const Color textLightGrey  = Color(0xFF8888AA);

  // ─── Primary — بنفسجي + وردي مبهر ──────────────────
  static const Color orange         = Color(0xFF7C3AED);
  static const Color orangeLight    = Color(0xFFA855F7);
  static const Color orangeDim      = Color(0x207C3AED);

  // ─── Secondary — وردي فيوشيا ────────────────────────
  static const Color blueFollow     = Color(0xFFEC4899);
  static const Color blueLight      = Color(0xFFF472B6);
  static const Color blueDim        = Color(0x20EC4899);

  // ─── Live / Battle — سماوي ──────────────────────────
  static const Color battleProgressColor = Color(0xFF06B6D4);
  static const Color battleDim      = Color(0x2006B6D4);

  // ─── Success — أخضر زمردي ───────────────────────────
  static const Color green          = Color(0xFF10B981);
  static const Color green1         = Color(0xFF34D399);
  static const Color greenDim       = Color(0x2010B981);

  // ─── Like / Error — أحمر برتقالي ────────────────────
  static const Color likeRed        = Color(0xFFFF4757);
  static const Color textStoryBgGradient2 = Color(0xFFFF6B81);
  static const Color redDim         = Color(0x20FF4757);

  // ─── Surfaces ───────────────────────────────────────
  static const Color bgLightGrey    = Color(0xFFF5F5FF);
  static const Color bgGrey         = Color(0xFFEAEAFF);
  static const Color bgMediumGrey   = Color(0xFFF0F0FF);
  static const Color disabledGrey   = Color(0xFFBBBBDD);
}
